const unverified = 0
const active = 1
const inActive = 2 // ban
const adminRole = 2
const userRole = 1
const projectFolderName = "tickfilm_api";

module.exports = {
  inActive,
  active,
  unverified,
  adminRole,
  userRole,
  projectFolderName
}